TASK — TABULAR MULTICLASS CLASSIFICATION (otto, 9 classes)

DATASET — Otto Group product classification; classify products into 9 categories from 93 anonymized non-negative count features (sparse, many zeros)

CONTRACT
```python
import numpy as np

class Model:
    def fit_predict(self, X_train, y_train, X_val, y_val, X_query) -> np.ndarray:
        ...
        return proba  # 2D float array, shape (len(X_query), n_classes); column j = P(class j)

def entrypoint() -> type:
    return Model
```
- Model() takes no constructor arguments
- all predictions finite (no NaN, no inf)

COLUMNS (assembled X[:, j], left to right)
- [0-92]     numeric
