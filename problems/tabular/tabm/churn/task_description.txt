TASK — TABULAR BINARY CLASSIFICATION (churn)

DATASET — predict whether a bank customer churns (closes their account) from credit, demographic, and account-activity attributes

CONTRACT
```python
import numpy as np

class Model:
    def fit_predict(self, X_train, y_train, X_val, y_val, X_query) -> np.ndarray:
        ...
        return proba  # 2D float array, shape (len(X_query), n_classes); column j = P(class j)

def entrypoint() -> type:
    return Model
```
- Model() takes no constructor arguments
- all predictions finite (no NaN, no inf)

COLUMNS (assembled X[:, j], left to right)
- [0] CreditScore customer credit score (350-850)
- [1] Age         customer age in years (18-92)
- [2] Tenure      years as a bank customer (0-10)
- [3] Balance     account balance (0-250898; many exact zeros)
- [4] NumOfProducts count of bank products held (1-4)
- [5] EstimatedSalary estimated annual salary (~11.6-199992)
- [6] EstimatedSalary_dup byte-identical duplicate of column 5 (EstimatedSalary)
- [7] Gender      1=Male (~54.5%), 0=Female
- [8] HasCrCard   1=holds a credit card (~70%), 0=no card
- [9] IsActiveMember 1=active member (~52%), 0=inactive
- [10] Geography   country of residence — categorical (integer code), 3 levels: 0=France 1=Germany 2=Spain

PROTOCOL
- (X_train, y_train) and (X_val, y_val) are both labeled and may both be used to fit; X_query is the unlabeled scoring slice — prediction quality on it is the only objective
- categorical columns are integer codes; train-derived code vocabularies are listed above when available, and unseen values use -1

CONSTRAINTS
- fix all random seeds (numpy + library random_state/random_seed/seed)
- no dataset peek (no fetch_*, openml, pickled caches)
- no print()/stdout in fit_predict; silence library logs
