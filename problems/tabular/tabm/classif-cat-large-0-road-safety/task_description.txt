TASK — TABULAR BINARY CLASSIFICATION (classif-cat-large-0-road-safety)

CONTRACT
```python
import numpy as np

class Model:
    def fit_predict(self, X_train, y_train, X_val, y_val, X_query) -> np.ndarray:
        ...
        return proba  # 2D float array, shape (len(X_query), n_classes); column j = P(class j)

def entrypoint() -> type:
    return Model
```
- Model() takes no constructor arguments
- all predictions finite (no NaN, no inf)

COLUMNS (assembled X[:, j], left to right)
- [0-28]     numeric
- [29]       categorical (integer code), 2 levels: 0=1 1=2
- [30]       categorical (integer code), 2 levels: 0=1 1=2
- [31]       categorical (integer code), 2 levels: 0=1 1=2
