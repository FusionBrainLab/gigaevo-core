TASK — TABULAR REGRESSION (house)

DATASET — 1990 US Census survey regions; predict regional median house price (USD, top-coded near 500001); the 16 numeric features are an anonymized, normalized census-attribute subset

CONTRACT
```python
import numpy as np

class Model:
    def fit_predict(self, X_train, y_train, X_val, y_val, X_query) -> np.ndarray:
        ...
        return preds  # 1D float array, shape (len(X_query),)

def entrypoint() -> type:
    return Model
```
- Model() takes no constructor arguments
- all predictions finite (no NaN, no inf)

COLUMNS (assembled X[:, j], left to right)
- [0-15]     numeric
