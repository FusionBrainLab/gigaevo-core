TASK — TABULAR BINARY CLASSIFICATION (tabarena-fitness-club)

CONTRACT
```python
import numpy as np

class Model:
    def fit_predict(self, X_train, y_train, X_val, y_val, X_query) -> np.ndarray:
        ...
        return proba  # 2D float array, shape (len(X_query), n_classes); column j = P(class j)

def entrypoint() -> type:
    return Model
```
- Model() takes no constructor arguments
- all predictions finite (no NaN, no inf)

COLUMNS (assembled X[:, j], left to right)
- [0] months_as_member numeric
- [1] weight      numeric
- [2] days_before numeric
- [3] day_of_week categorical (integer code), 7 levels: 0=Fri 1=Mon 2=Sat 3=Sun 4=Thu 5=Tue 6=Wed
- [4] time        categorical (integer code), 2 levels: 0=AM 1=PM
- [5] category    categorical (integer code), 6 levels: 0=- 1=Aqua 2=Cycling 3=HIIT 4=Strength 5=Yoga
