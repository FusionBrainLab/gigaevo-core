TASK — TABULAR REGRESSION (tabarena-another-dataset-on-used-fiat-500)

CONTRACT
```python
import numpy as np

class Model:
    def fit_predict(self, X_train, y_train, X_val, y_val, X_query) -> np.ndarray:
        ...
        return preds  # 1D float array, shape (len(X_query),)

def entrypoint() -> type:
    return Model
```
- Model() takes no constructor arguments
- all predictions finite (no NaN, no inf)

COLUMNS (assembled X[:, j], left to right)
- [0] engine_power numeric
- [1] age_in_days numeric
- [2] km          numeric
- [3] previous_owners numeric
- [4] lat         numeric
- [5] lon         numeric
- [6] model       categorical (integer code), 3 levels: 0=lounge 1=pop 2=sport
