TASK — TABULAR REGRESSION (tabarena-healthcare-insurance-expenses)

CONTRACT
```python
import numpy as np

class Model:
    def fit_predict(self, X_train, y_train, X_val, y_val, X_query) -> np.ndarray:
        ...
        return preds  # 1D float array, shape (len(X_query),)

def entrypoint() -> type:
    return Model
```
- Model() takes no constructor arguments
- all predictions finite (no NaN, no inf)

COLUMNS (assembled X[:, j], left to right)
- [0] age         numeric
- [1] bmi         numeric
- [2] children    numeric
- [3] sex         categorical (integer code), 2 levels: 0=female 1=male
- [4] smoker      categorical (integer code), 2 levels: 0=no 1=yes
- [5] region      categorical (integer code), 4 levels: 0=northeast 1=northwest 2=southeast 3=southwest
