**TASK DEFINITION – HEXAGON PACKING OPTIMIZER**

**Challenge:** Implement a Python class that minimizes the side length of a flat-topped regular enclosing hexagon required to contain $N$ non-overlapping unit regular hexagons.

**Specific Benchmark:** $N=23$.
**General Requirement:** The code must be valid for any $N$ from 1 to 25.

**GEOMETRIC SPECIFICATIONS**
1.  **Unit Hexagons (Items):**
    *   Regular hexagons with circumradius $r = 1.0$.
    *   Side length = 1.0.
    *   May be arbitrarily placed $(x, y)$ and rotated $(\theta)$.
2.  **Enclosing Hexagon (Container):**
    *   Regular, flat-topped, centered at $(0,0)$.
    *   "Flat-topped" vertices are located at angles $[0^\circ, 60^\circ, 120^\circ, 180^\circ, 240^\circ, 300^\circ]$ relative to the center.
    *   Objective: Minimize the enclosing hexagon's side length $L$ (which is equal to its circumradius).

**OBJECTIVE FUNCTION**
Maximize Fitness: $-L$ (Minimize side length $L$).
*Target for N=23:* $L \le 5.39$.

**CONSTRAINTS**
1.  **Containment:** All vertices of every unit hexagon must lie inside or on the boundary of the enclosing hexagon.
2.  **Non-overlap:** The intersection area between any pair of unit hexagons must be zero (touching is allowed).
3.  **Count:** Exactly `hex_num` unit hexagons must be placed.

**EVALUATION BUDGET**
One evaluation gives your class ONE total wall-clock budget. The evaluator calls it repeatedly until that budget is spent, independently validates every configuration it hands back, and keeps the best valid one. Nothing else is rationed, so the cost of a call is a real cost: a cheap call buys more calls, and an expensive call has to earn its keep by being better. Time spent inside one call is time no other call can use.

**IMPLEMENTATION REQUIREMENTS**

**Libraries:**
Use `numpy`, `scipy`, `sklearn`, `jax`, `optax`, `jaxopt` or standard Python libraries.

**Class Interface:**
You must implement the following class structure exactly:

Implement a Python class with one method:

propose(input_config, intensity, seed=None)

- If input_config is None, propose a starting configuration.
- Otherwise, return a candidate derived from the supplied incumbent.
- intensity controls the scale of the attempted basin transition.
- The method may internally initialize, perturb, locally optimize, or combine these operations.
- The outer evaluator independently validates every candidate and accepts only feasible improvements.

```python
import numpy as np

class Proposer:
    def __init__(self, hex_num=23, seed: int = 0):
        self.hex_num = hex_num
        self.seed = seed
        # Initialize RNG or JAX keys here

    def propose(self, input_config: tuple[np.ndarray, np.ndarray] | None, intensity: float, seed=None) -> tuple[np.ndarray, np.ndarray]:
        """
        Returns a candidate configuration.
        If input_config is None, propose a starting configuration.
        Otherwise, return a candidate derived from the supplied incumbent.
        May internally initialize, perturb, locally optimize, or combine these.
        Must accept valid OR invalid inputs and push them toward validity and optimality.

        Args:
            input_config: Tuple (centers, angles), or None
            intensity: Float (1e-3 to 1e3). Higher intensity = more drastic structural changes.
            seed: Optional seed

        Returns:
            centers: np.ndarray of shape (hex_num, 2)
            angles: np.ndarray of shape (hex_num,) in radians [0, 2π)
        """
        pass

def entrypoint():
    return Proposer
```

**CRITICAL FAILURE MODES**
*   **Wrong Shapes:** Returning arrays of shape `(24, ...)` when `hex_num=23`.
*   **Geometry Errors:** Assuming point-topped enclosing hexagon instead of flat-topped.
*   **Invalid Output:** Hexagons overlapping or protruding outside the boundary in the final result.

**DOCUMENTATION**
Supply your code with a brief explanation of the algorithm using LaTeX in the introductory comment.