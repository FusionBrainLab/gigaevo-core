# @package _global_

program_format:
  id: python_source
  # Normal GigaEvo programs are Python files. The default pipeline keeps
  # ValidateCodeStage + CallProgramFunction unchanged for this format.
  evaluation_feature: null
