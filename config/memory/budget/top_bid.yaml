# @package memory.budget
# Bid-ranked budgeter (pairs with `memory/auction=thompson_ev`). Caps winners
# to max_cards by realized EV bid (theta x magnitude), not the gate's theta.
_target_: gigaevo.memory.read.auction.TopBidBudgeter
